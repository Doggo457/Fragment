<Window x:Class="ClipForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ClipForge"
        Width="460"
        Height="560"
        MinWidth="420"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Fallback brushes in case App.xaml resources are unavailable at design time. -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FF1A1C20" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FF24262C" />
        <SolidColorBrush x:Key="ForegroundBrush" Color="#FFE8E8E8" />
        <SolidColorBrush x:Key="MutedForegroundBrush" Color="#FF9AA0A6" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF00B3FF" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FFE5484D" />
        <SolidColorBrush x:Key="BorderBrush" Color="#FF34373E" />

        <!-- Flat dark button style. -->
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF2E3138" />
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF3A3D45" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Large primary record button style. -->
        <Style x:Key="RecordButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="#FF0B1014" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="120" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="60">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.78" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- Turn red while recording. -->
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
                    <Setter Property="Foreground" Value="#FFFFFFFF" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedForegroundBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="2,0,0,4" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- Header -->
            <RowDefinition Height="Auto" />   <!-- Record button -->
            <RowDefinition Height="Auto" />   <!-- Status + timer -->
            <RowDefinition Height="Auto" />   <!-- Profile -->
            <RowDefinition Height="Auto" />   <!-- Replay + Save Clip -->
            <RowDefinition Height="*" />      <!-- Spacer -->
            <RowDefinition Height="Auto" />   <!-- Action buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,18">
            <Ellipse Width="14" Height="14" Fill="{DynamicResource AccentBrush}" VerticalAlignment="Center" />
            <TextBlock Text="ClipForge"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Large Start/Stop record button -->
        <Button Grid.Row="1"
                Width="120"
                Style="{StaticResource RecordButton}"
                Command="{Binding StartStopCommand}"
                Content="{Binding RecordButtonText}"
                HorizontalAlignment="Center"
                Margin="0,0,0,18" />

        <!-- Live status + timer -->
        <Border Grid.Row="2"
                Background="{DynamicResource PanelBackgroundBrush}"
                CornerRadius="8"
                Padding="14,12"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="STATUS" Style="{StaticResource SectionLabel}" />
                    <TextBlock Text="{Binding StatusText}"
                               FontSize="15"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Text="ELAPSED" Style="{StaticResource SectionLabel}" HorizontalAlignment="Right" />
                    <TextBlock Text="{Binding RecordTimer}"
                               FontSize="22"
                               FontWeight="SemiBold"
                               FontFamily="Consolas"
                               Foreground="{DynamicResource AccentBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Profile selection -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="PROFILE" Style="{StaticResource SectionLabel}" />
            <ComboBox ItemsSource="{Binding ProfileNames}"
                      SelectedItem="{Binding SelectedProfileName, Mode=TwoWay}" />
        </StackPanel>

        <!-- Replay toggle + Save Clip -->
        <Grid Grid.Row="4" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ToggleButton Grid.Column="0"
                          Height="44"
                          Cursor="Hand"
                          IsChecked="{Binding IsReplayRunning, Mode=OneWay}"
                          Command="{Binding ToggleReplayCommand}"
                          Foreground="{DynamicResource ForegroundBrush}">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{DynamicResource PanelBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter Property="Foreground" Value="#FF0B1014" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ToggleButton.Template>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Replay Buffer" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding ReplayStateText}" Margin="6,0,0,0" Opacity="0.8" />
                </StackPanel>
            </ToggleButton>

            <Button Grid.Column="2"
                    Height="44"
                    Style="{StaticResource FlatButton}"
                    Command="{Binding SaveClipCommand}"
                    Content="Save Clip" />
        </Grid>

        <!-- Bottom action buttons -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Style="{StaticResource FlatButton}"
                    Command="{Binding OpenSettingsCommand}"
                    Content="Settings" />
            <Button Grid.Column="2"
                    Style="{StaticResource FlatButton}"
                    Command="{Binding OpenTrimmerCommand}"
                    Content="Trim" />
            <Button Grid.Column="4"
                    Style="{StaticResource FlatButton}"
                    Command="{Binding OpenOutputFolderCommand}"
                    Content="Output Folder" />
        </Grid>
    </Grid>
</Window>
